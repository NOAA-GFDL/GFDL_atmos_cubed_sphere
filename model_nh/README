The same file logic is on both directories.

The 32-bit version has been temporarily separated into two files to work around an Intel v16 bug in which 32-bit AVX2 instructions don't give run-to-run reproducibility.
